{
  "name": "eoq-chatbot",
  "version": "1.0.0",
  "private": true,
  "description": "Chatbot EOQ - Modelo de Wilson. Un solo comando crea el venv, instala dependencias y levanta backend + frontend.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:back": "python -m uvicorn backend.main:app --reload",
    "dev:front": "npm --prefix frontend run dev"
  }
}
